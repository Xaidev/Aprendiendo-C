/*
    Ciclo for en C
*/
#include <stdio.h>
int main()
{
    int contador;
    for (contador = 9; contador <= 10; contador++)
    {
        // Mostraremos que pasa con contador durante el ciclo
        printf("%i\n", contador);
    }
    return 0; // termina el programa
}